akka {
	loglevel = "debug"
}

scala-ldap-server {
	logASN1 = false
	logLDAPRequest = true
	logLDAPResponse = true
	
	host = "localhost"
	port = 1389
	base = "dc=example,dc=com"
	mongo {
		hosts = ["localhost"]
		dbName = "ldap"
	}
}